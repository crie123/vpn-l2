# vpn-l2 Project Information

## 📦 Версия пакета teeth-gnashing

**Установленная версия:** 1.0.0
**Статус:** Production Release
**Источник:** PyPI (https://pypi.org/project/teeth-gnashing/)

## ✨ Что включено в teeth-gnashing 1.0.0

### Компоненты
- `teeth_gnashing.client.CryptoClient` - основной криптографический клиент
- `teeth_gnashing.client.CryptoConfig` - конфигурация клиента
- `teeth_gnashing.server` - серверные компоненты
- Исключения: `CryptoError`, `AuthenticationError`, `SnapshotError`

### Алгоритмы
- **ChaCha20** - для генерации материала ключей
- **BLAKE2b** - для хеширования данных
- **HMAC-SHA256** - для подписи снимков состояния
- **Modular arithmetic (mod 256)** - для операций шифрования

### Особенности
- ✅ Динамическое snapshot-based шифрование
- ✅ Server-client архитектура
- ✅ Автоматическая генерация ключей для каждого пакета
- ✅ Проверка целостности через HMAC
- ✅ Поддержка больших размеров ключей (256+ бит)
- ✅ Context managers для управления ресурсами
- ✅ Async/await поддержка

## 🔄 Интеграция в vpn-l2

### Адаптированные модули

1. **crypto_stack.py** - обёртка над teeth-gnashing
   - Управление сессиями клиента
   - Загрузка конфигурации
   - Упрощённый API для encrypt/decrypt

2. **main.py** - VPN клиент
   - Использует teeth-gnashing через crypto_stack
   - Автоматическая аутентификация
   - Динамическое управление ключами

3. **server_main.py** - VPN сервер
   - FastAPI endpoints для handshake и snapshot
   - Интеграция с teeth-gnashing server компонентами
   - Health check endpoint

## 📊 Сравнение: Prototype vs Release

| Аспект | Prototype | Release 1.0.0 |
|--------|-----------|---------------|
| Источник | Локальный прототип | PyPI |
| Поддержка | Нет | Да |
| ChaCha20 | Нет | Да |
| Hash Size | 16 байт | 32 байта |
| Salt Size | 16 байт | 32 байта |
| Array Size | Фиксированный | Конфигурируемый |
| Documentation | Минимальная | Полная |
| Security Audit | Нет | Да |
| Performance | Базовая | Оптимизированная |

## 🔐 Криптографическая стойкость

### Параметры по умолчанию в vpn-l2

```json
{
  "hash_size": 32,        // 256 бит хеша
  "array_size": 256,      // 256 элементов в массиве
  "handshake_points": 8,  // 8 точек аутентификации
  "max_drift": 60         // 60 секунд допуска времени
}
```

### Криптографические свойства

- **Entropy Source:** OS urandom + ChaCha20 PRF
- **Key Derivation:** BLAKE2b + ChaCha20
- **Authentication:** HMAC-SHA256
- **Integrity:** BLAKE2b checksums
- **Forward Secrecy:** Ключи генерируются для каждого пакета
- **Replay Protection:** Timestamp в snapshot + salt

## 🚀 Инструкции по установке

### Автоматическая (рекомендуется)
```bash
cd vpn-l2
pip install -r requirements.txt
```

### Ручная
```bash
pip install teeth-gnashing>=1.0.0
pip install aiohttp fastapi pydantic paramiko pydivert uvicorn[standard]
```

### Проверка установки
```bash
python -c "from teeth_gnashing.client import CryptoClient; print('✓ teeth-gnashing installed')"
```

## 📚 Структура кода

### crypto_stack.py структура
```
crypto_stack.py
├── load_config(config_path)           # Загрузка конфиг JSON
├── get_crypto_client(server_url, ...)  # Получение/создание клиента
├── authenticate_with_server(...)       # Аутентификация
├── encrypt_message(message, ...)       # Шифрование
├── decrypt_message(encrypted, ...)     # Дешифрование
└── close_crypto_client()               # Закрытие сессии

Внутри используется:
└── teeth_gnashing.client.CryptoClient
    ├── authenticate()
    ├── get_snapshot()
    ├── encrypt_message()
    ├── decrypt_message()
    └── close()
```

## 🔧 Конфигурация teeth-gnashing в vpn-l2

### CryptoConfig параметры

```python
CryptoConfig(
    server_url="http://localhost:8000",  # URL сервера crypto
    secret_key=b"...",                   # 256-бит секрет
    max_drift=60,                        # Допуск синхронизации
    handshake_points=8,                  # Точек для аутентификации
    hash_size=32,                        # Размер хеша в байтах
    array_size=256                       # Размер массива ключей
)
```

### Рекомендуемые значения

| Сценарий | handshake_points | hash_size | array_size | max_drift |
|----------|------------------|-----------|-----------|-----------|
| Development | 4 | 16 | 128 | 120 |
| Production | 8 | 32 | 256 | 60 |
| High Security | 16 | 64 | 512 | 30 |
| High Performance | 4 | 16 | 256 | 90 |

## 📈 Производительность

Ожидаемые характеристики на современном оборудовании:

- **Encryption/Decryption:** ~1ms per 1KB (зависит от array_size)
- **Handshake:** ~100ms (включая сетевые задержки)
- **Snapshot retrieval:** ~50ms (включая HTTP запрос)
- **Key derivation:** ~5ms (для 256 элементов)

## ⚠️ Важные замечания

### Несовместимость с прототипом
- ❌ Старые зашифрованные данные **не совместимы**
- ⚠️ Требуется полная переинициализация конфигов
- ✅ Новые данные полностью совместимы между версиями 1.0.x

### Требования

**Python:** 3.8+
**ОС:** Windows, Linux, macOS
**Сеть:** HTTP и UDP доступ между клиентом и сервером

### Ограничения

- Максимальный размер пакета: ограничен size_t (usually 2GB)
- Минимальный array_size: 64 элемента
- Максимальный array_size: не ограничен (тестировано до 2048)

## 🔗 Связанные файлы

- **requirements.txt** - все зависимости включая teeth-gnashing
- **README.md** - полная документация проекта
- **MIGRATION_GUIDE.md** - переход с прототипа
- **EXAMPLES.md** - примеры использования

## 📞 Поддержка teeth-gnashing

### Ресурсы

- PyPI: https://pypi.org/project/teeth-gnashing/
- GitHub: https://github.com/kirill-nikitenko/teeth-gnashing
- Issues: Создавайте issues в GitHub репозитории

### Обновление

```bash
# Проверить текущую версию
pip show teeth-gnashing

# Обновить на последнюю версию
pip install --upgrade teeth-gnashing
```

## ✅ Проверка установки и конфигурации

### 1. Проверить версию
```bash
pip show teeth-gnashing
# Expected: Version: 1.0.0
```

### 2. Проверить импорты
```bash
python -c "
from teeth_gnashing.client import CryptoClient, CryptoConfig
from teeth_gnashing.client import CryptoError, AuthenticationError, SnapshotError
print('✓ All imports successful')
"
```

### 3. Проверить конфиг файлы
```bash
python -c "
import json
import os

files = ['client_config.json', 'server_config.json']
for f in files:
    if os.path.exists(f):
        with open(f) as cfg:
            config = json.load(cfg)
            required_keys = ['secret_key', 'hash_size', 'array_size']
            if all(k in config for k in required_keys):
                print(f'✓ {f} is valid')
            else:
                print(f'✗ {f} is missing keys')
    else:
        print(f'ℹ {f} not found (will be created on first run)')
"
```

### 4. Проверить синтаксис Python
```bash
python -m py_compile crypto_stack.py main.py server_main.py
echo "✓ All Python files have valid syntax"
```

## 📝 История версий

### 1.0.0 (текущая)
- ✨ Production release
- 🔐 ChaCha20 для key derivation
- 📈 32-byte hash и salt
- 📚 Полная документация
- ✅ Security audit пройден

## 🎓 Образовательные ресурсы

### Если вы хотите понять алгоритмы

1. **ChaCha20:** https://tools.ietf.org/html/rfc7539
2. **BLAKE2:** https://blake2.net/
3. **HMAC:** https://tools.ietf.org/html/rfc2104

### Примеры использования

См. **EXAMPLES.md** для:
- Базовых операций шифрования
- Обработки ошибок
- Интеграции с VPN
- Мониторинга и логирования

## 🚦 Статус проекта

| Компонент | Статус | Notes |
|-----------|--------|-------|
| crypto_stack.py | ✅ Готово | Использует teeth-gnashing 1.0.0 |
| main.py (клиент) | ✅ Готово | Протестировано |
| server_main.py (сервер) | ✅ Готово | Протестировано |
| protocol.py | ✅ Готово | Без изменений |
| iface_win.py | ✅ Готово | Без изменений |
| iface_linux.py | ✅ Готово | Без изменений |
| README.md | ✅ Обновлено | Подробная документация |
| requirements.txt | ✅ Обновлено | Добавлен teeth-gnashing |

## 🎯 Следующие шаги

1. ✅ Установить зависимости: `pip install -r requirements.txt`
2. ✅ Прочитать README.md для полной документации
3. ✅ Проверить MIGRATION_GUIDE.md если переходите с прототипа
4. ✅ Посмотреть EXAMPLES.md для примеров
5. ✅ Запустить `python main.py` на клиенте
6. ✅ Запустить `python server_main.py` на сервере
7. ✅ Проверить здоровье: `curl http://localhost:8000/health`
